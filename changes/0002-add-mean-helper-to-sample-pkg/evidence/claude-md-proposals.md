<!-- Proposed CLAUDE.md lines under "Things Claude gets wrong" (article p.34 step 5). The PR description quotes this file; only the owner edits CLAUDE.md. -->
- CLAUDE.md's Layout entries for sample_pkg/calc.py (line 22) and sample_pkg/__init__.py (line 23) still list only add, divide and percent, so the guardrail file is now outdated about the package's public API now that mean ships. The run cannot fix this itself (CLAUDE.md is a guardrail file only the owner may edit); the fix is proposed for the owner in evidence/claude-md-proposals.md, which this commit correctly narrowed to the plain first-time flag after the phase-e panel (decisions-e.md item 1) found the earlier 'second occurrence' framing unsupported by findings.py's own update_seen logic (repeated only fires when a signature's first_change differs from the current change id, which is not the case for either of 0002's own CLAUDE.md signatures in changes/.review-seen.json) (change 0002; the owner edits CLAUDE.md)
